flask==3.0.3
gunicorn==22.0.0

# FIX: python:3.12-slim не включает setuptools.
# setuptools>=80 удалил pkg_resources, который требует opentelemetry-instrumentation.
setuptools<80

# OpenTelemetry — версии проверены на PyPI (март 2025)
opentelemetry-api==1.39.1
opentelemetry-sdk==1.39.1
opentelemetry-instrumentation-flask==0.60b1
opentelemetry-exporter-otlp-proto-grpc==1.39.1

# Tests (только для CI, не попадают в финальный образ)
pytest==8.3.5
pytest-cov==6.0.0
